/* EC@RT — Zone 02 Background — config Unity */
window.addEventListener("load", () => {
  ECARTLoader.init({
    buildPath: "../../unity/zone02-V2/Build",
    buildName: "build-mamco_compress"
  });
});
